kind: Service
apiVersion: v1
metadata:
  name: wordpress-service
  namespace: wordpress-app
spec:
  type: NodePort
  selector:
    app: wordpress
  ports:
   - port: 80
     targetPort: 80
     nodePort: 30080 # will allocate a port from a range (default: 30000-32767)
